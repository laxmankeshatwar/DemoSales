name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore SalesApi/SalesApi.csproj

      - name: Build
        run: dotnet build SalesApi/SalesApi.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish SalesApi/SalesApi.csproj -c Release -o publish --no-restore

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: sales-api-demo-123
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: publish
